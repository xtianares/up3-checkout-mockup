module.exports = {
  sass                    : require('./css').sass,
  bootstrapCss            : require('./css').bootstrapCss,
  criticalCss             : require('./css').criticalCss,
  rootFiles               : require('./general').rootFiles,
  sitemap                 : require('./general').sitemap,
  generateServiceWorker   : require('./general').generateServiceWorker,
  set_dl_env              : require('./general').set_dl_env,
  set_ml_env              : require('./general').set_ml_env,
  set_prod_env            : require('./general').set_prod_env,
  clean                   : require('./general').clean,
  watch                   : require('./general').watch,
  webServer               : require('./general').webServer,
  html                    : require('./html').html,
  moveIndex               : require('./html').moveIndex,
  imagemin                : require('./image').imagemin,
  imagewebp               : require('./image').imagewebp,
  jsConcat                : require('./javascript').jsConcat,
  jsCopy                  : require('./javascript').jsCopy,
}
